// CSV utilities — stub for mobile (import/export handled via web app)
export function parseCSV(_csv: string): unknown[] {
  return [];
}

export function exportCSV(_data: unknown[]): string {
  return "";
}
